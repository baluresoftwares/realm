export interface UnauthorizedContentProps {
    isDarkMode: boolean;
	lockPaths: any[]
}

export interface UnauthorizedHeaderProps {
    companyName: string;
    logoUrl: string;
    label: string;
    isDarkMode: boolean;
}